apiVersion: v1
kind: Service
metadata:
  name: factory-api
  namespace: factory
  labels:
    app.kubernetes.io/name: factory-api
spec:
  selector:
    app.kubernetes.io/name: factory-api
  ports:
    - name: http
      port: 80
      targetPort: http
